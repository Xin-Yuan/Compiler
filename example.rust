/*
 * Example with Functions
 */

// variables

let mut b[int, 10];
let mut c:float = 1.6;
let a = "ppp";
let mut c:float = 1.1;
    
// function declaration
fn add(a:int, b:int) -> int
{
  let mut c:float = 1;
  a = a + 1;
  return a+b;
}
  
// main function
fn main() 
{
  c = add(a, 10);
  if (c > 10) {
    print -c;
  }
  else {
    print c;
  }
  println ("Hello World");
}

